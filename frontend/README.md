# Frontend — Meals UI

Install and run:

```bash
cd frontend
npm install
npm run dev
```

Set API base URL in `frontend/.env`:

```env
VITE_API_BASE=http://localhost:4000
```

This is a minimal React + Vite app with a meal request form and Arabic/English toggle.
