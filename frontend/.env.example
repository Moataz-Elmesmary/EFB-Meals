# Backend API base URL
VITE_API_BASE=http://localhost:4000

# Note: Microsoft SSO is configured on the BACKEND (.env → AZURE_CLIENT_ID /
# AZURE_TENANT_ID). The frontend reads it from /api/config automatically.
# When those are unset, the app runs in demo sign-in mode.
